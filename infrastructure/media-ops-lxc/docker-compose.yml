networks:
  immich-net:
  nextcloud-net:

services:
  # Grafana Alloy — ships this LXC's Docker container logs to central Loki on CT405.
  alloy:
    image: grafana/alloy:v1.17.1
    container_name: alloy
    restart: unless-stopped
    command:
      - run
      - --server.http.listen-addr=0.0.0.0:12345
      - --storage.path=/var/lib/alloy/data
      - /etc/alloy/config.alloy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./alloy/config.alloy:/etc/alloy/config.alloy:ro
      - /var/lib/alloy:/var/lib/alloy
    ports:
      - "12345:12345"  # Alloy UI (debug)

  node-exporter:
    image: prom/node-exporter:v1.11.1
    container_name: node-exporter
    restart: unless-stopped
    ports:
      - "9100:9100"
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - "--path.procfs=/host/proc"
      - "--path.rootfs=/rootfs"
      - "--path.sysfs=/host/sys"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"

  # ============================================================
  # PHOTO MANAGEMENT (Immich)
  # ============================================================
  immich-server:
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    container_name: immich-server
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /mnt/das/immich/upload:/usr/src/app/upload
      - /etc/localtime:/etc/localtime:ro
      - /mnt/das/nextcloud/external_locaions/Raid-Shared/shared/shared_photos:/usr/src/app/external/deepkhush_photos
      - /mnt/das/nextcloud/external_locaions/Raid-Shared/shared/deep/Deepkhush Photos:/usr/src/app/external/shared_photos
      # External library: Kashyap & Shradhha photos from the WD drive (read-only).
      # Host /mnt/wd-ext-hdd is bind-mounted into CT202 (mp2); Immich scans it via
      # the in-container path below (add it under Admin -> External Libraries).
      - "/mnt/wd-ext-hdd/Kashyap & Shradhha:/usr/src/app/external/kashyap_shradhha:ro"
    env_file:
      - .env
    ports:
      - "2283:2283"
    depends_on:
      - immich-db
      - immich-redis
    networks:
      - immich-net
      - default
    restart: unless-stopped

  immich-machine-learning:
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}-rocm
    container_name: immich-ml
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /mnt/das/immich/model-cache:/cache
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
    networks:
      - immich-net
    restart: unless-stopped

  immich-db:
    # Immich's own Postgres image — PG major + vectorchord/pgvector versions are
    # tied to the Immich server release. This is what Immich v3.0.x ships: still
    # PostgreSQL 14 (NO major-version migration), VectorChord bumped 0.3.0->0.4.3
    # (applied by Immich's startup DB migrations on the existing pg14 data dir).
    # WUD will flag "18-vectorchord...-pgvector..." — that's a FUTURE PG14->18
    # track needing a coordinated pg_upgrade/dump-restore; never take it via a
    # bare `docker compose pull`. The label keeps WUD on the pg14-vectorchord line.
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    container_name: immich-db
    labels:
      - wud.tag.include=^14-vectorchord.*
    volumes:
      - /var/lib/immich-db:/var/lib/postgresql/data
    env_file:
      - .env
    networks:
      - immich-net
    restart: unless-stopped

  immich-redis:
    image: redis:7-alpine
    container_name: immich-redis
    # WUD default sorting picks the junk tag "32bit-stretch" as "newer" than
    # 7-alpine. Restrict the watch to real N[-.N]-alpine tags so the false
    # positive stops showing (and we never pull a 32-bit Debian-stretch Redis).
    labels:
      - wud.tag.include=^\d+(\.\d+)*-alpine$
    networks:
      - immich-net
    restart: unless-stopped

  # ============================================================
  # PRIVATE CLOUD (Nextcloud)
  # ============================================================
  nextcloud:
    image: nextcloud:stable
    container_name: nextcloud
    # Docker Hub :stable — WUD needs this to watch the digest (see WUD note).
    labels:
      - wud.watch.digest=true
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /var/www/nextcloud/:/var/www/html
      - /var/www/nextcloud/custom_apps:/var/www/html/custom_apps
      - /var/www/nextcloud/config:/var/www/html/config
      - /mnt/das/nextcloud/data:/var/www/html/data
      - /mnt/das/nextcloud/external_locaions/tmpfs:/tmpfs
      - /mnt/das/nextcloud/external_locaions/Non-Raid-1:/var/SG_NonRaid
      - /mnt/das/nextcloud/external_locaions/Non-Raid-2:/var/WD_NonRaid
      - /mnt/das/nextcloud/external_locaions/Raid-Shared:/var/Raid
      - /tmp:/tmp:exec
      - /mnt/sg-ext-hdd:/var/sg-ext-hdd
      - /mnt/wd-ext-hdd:/var/wd-ext-hdd
    environment:
      - MYSQL_HOST=mariadb
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=${NC_DB_USER}
      - MYSQL_PASSWORD=${NC_DB_PASSWORD}
      - NEXTCLOUD_TRUSTED_DOMAINS=${NC_TRUSTED_DOMAINS}
      - OVERWRITECLIURL=${NC_OVERWRITE_URL}
      - OVERWRITEHOST=${NC_OVERWRITE_HOST}
      - OVERWRITEPROTOCOL=https
      - PHP_MEMORY_LIMIT=2G
      - PHP_UPLOAD_LIMIT=100G
      - APACHE_BODY_LIMIT=0
      - REDIS_HOST=nextcloud-redis
      - REDIS_PORT=6379
    ports:
      - "8080:80"
    depends_on:
      - mariadb
      - nextcloud-redis
    networks:
      - nextcloud-net
      - default
    restart: unless-stopped

  mariadb:
    image: linuxserver/mariadb:latest
    container_name: mariadb
    # Docker Hub :latest — WUD needs this to watch the digest.
    labels:
      - wud.watch.digest=true
    volumes:
      - /var/lib/nextcloud-db:/config
    environment:
      - MYSQL_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=${NC_DB_USER}
      - MYSQL_PASSWORD=${NC_DB_PASSWORD}
      - TZ=America/Chicago
    networks:
      - nextcloud-net
    restart: unless-stopped

  nextcloud-redis:
    image: redis:7-alpine
    container_name: nextcloud-redis
    # Same WUD false positive as immich-redis (junk "32bit-stretch" tag).
    labels:
      - wud.tag.include=^\d+(\.\d+)*-alpine$
    networks:
      - nextcloud-net
    restart: unless-stopped

  # Disabled 2026-05-30: switched Nextcloud to AJAX cron mode (`php occ background:ajax`).
  # User opens Nextcloud a few times a week — running cron every 5 min generated
  # constant CPU/iowait/temp spikes on Peladn for no benefit. AJAX mode runs
  # background jobs piggybacked on page loads, which matches the actual usage.
  # Re-enable this block (and revert to `background:cron` via occ) if you ever
  # add federated shares, calendar sharing, or other features that need
  # background work to happen while no one is logged in.
  # nextcloud-cron:
  #   image: nextcloud:stable
  #   container_name: nextcloud-cron
  #   entrypoint: /cron.sh
  #   volumes:
  #     - /var/www/nextcloud/:/var/www/html
  #     - /var/www/nextcloud/custom_apps:/var/www/html/custom_apps
  #     - /var/www/nextcloud/config:/var/www/html/config
  #     - /mnt/das/nextcloud/data:/var/www/html/data
  #     - /mnt/das/nextcloud/external_locaions/tmpfs:/tmpfs
  #     - /mnt/das/nextcloud/external_locaions/Non-Raid-1:/var/SG_NonRaid
  #     - /mnt/das/nextcloud/external_locaions/Non-Raid-2:/var/WD_NonRaid
  #     - /mnt/das/nextcloud/external_locaions/Raid-Shared:/var/Raid
  #     - /tmp:/tmp:exec
  #     - /mnt/sg-ext-hdd:/var/sg-ext-hdd
  #     - /mnt/wd-ext-hdd:/var/wd-ext-hdd
  #   environment:
  #     - MYSQL_HOST=mariadb
  #     - MYSQL_DATABASE=nextcloud
  #     - MYSQL_USER=${NC_DB_USER}
  #     - MYSQL_PASSWORD=${NC_DB_PASSWORD}
  #     - REDIS_HOST=nextcloud-redis
  #     - REDIS_PORT=6379
  #     - PHP_MEMORY_LIMIT=2G  # match main nextcloud service; default 512M OOMs on thumbnails
  #   depends_on:
  #     - nextcloud
  #     - mariadb
  #     - nextcloud-redis
  #   networks:
  #     - nextcloud-net
  #   restart: unless-stopped

  # ============================================================
  # MEDIA PLAYER (Jellyfin)
  # ============================================================
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    # Docker Hub :latest — WUD needs this to watch the digest.
    labels:
      - wud.watch.digest=true
    user: "1000:1000"
    group_add:
      - "44"   # video
      - "992"  # render
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /mnt/das/jellyfin/config:/config
      - /mnt/das/jellyfin/cache:/cache
      - /mnt/sg-ext-hdd/Entertainment/Movies:/media/movies
      - /mnt/sg-ext-hdd/Entertainment/tvshows:/media/shows
      - /mnt/sg-ext-hdd/Entertainment/songs:/media/songs
    ports:
      - "8096:8096"
    environment:
      - TZ=America/Chicago
    restart: unless-stopped

  # ============================================================
  # NVR (Frigate) — REMOVED in Phase 13
  # Replaced by native Reolink integration + HA automations
  # ============================================================
  # frigate:
  #   image: ghcr.io/blakeblackshear/frigate:stable
  #   container_name: frigate
  #   shm_size: "256mb"
  #   devices:
  #     - /dev/dri:/dev/dri
  #   volumes:
  #     - /mnt/das/frigate/config:/config
  #     - /mnt/das/frigate/media:/media/frigate
  #     - /etc/localtime:/etc/localtime:ro
  #   ports:
  #     - "5000:5000"
  #     - "8554:8554"
  #     - "8555:8555/tcp"
  #     - "8555:8555/udp"
  #     - "8971:8971"
  #   environment:
  #     - TZ=America/Chicago
  #     - GO2RTC_ALLOW_ARBITRARY_EXEC=true
  #     - OMP_NUM_THREADS=8
  #   restart: unless-stopped

  # ============================================================
  # PRIVATE BROWSER DESKTOP (Webtop)
  # ============================================================
  webtop:
    image: lscr.io/linuxserver/webtop:latest
    container_name: webtop
    devices:
      - /dev/dri:/dev/dri
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
      - CUSTOM_USER=${WEBTOP_USER}
      - PASSWORD=${WEBTOP_PASSWORD}
    volumes:
      # Config moved 2026-05-30 from /mnt/wd-ext-hdd/webtop/config (USB-3 HDD)
      # to /var/lib/webtop/config (NVMe). USB-HDD random I/O on browser cache
      # was causing stream stutter; NVMe eliminates it. Old USB dir kept as
      # backup until verified working.
      - /var/lib/webtop/config:/config
      - /mnt/wd-ext-hdd/webtop/downloads:/config/Downloads
    ports:
      - "3000:3000"
      - "3001:3001"
    shm_size: "1gb"
    ulimits:
      # No core dumps. Webtop crashes left 1.4 GB of stale core files in
      # /config root (2026-05-30). Disable to keep config dir clean.
      core: 0
    restart: unless-stopped

  # ============================================================
  # IMAGE-UPDATE MONITOR (What's Up Docker)
  # ============================================================
  # Watches every local container for newer image tags/digests and exposes an
  # API (/api/containers) that the Homepage dashboard's "whatsupdocker" widget
  # reads. Host port 3003 -> container 3000. Socket mounted read-only.
  wud:
    image: getwud/wud:latest
    container_name: wud
    # Docker Hub :latest — WUD watches its OWN update digest too.
    labels:
      - wud.watch.digest=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "3003:3000"
    environment:
      - TZ=America/Chicago
      - WUD_WATCHER_LOCAL_WATCHBYDEFAULT=true
    restart: unless-stopped