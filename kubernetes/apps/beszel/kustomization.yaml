apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: beszel
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - agent-daemonset.yaml
#patchesStrategicMerge: []
#patches: []